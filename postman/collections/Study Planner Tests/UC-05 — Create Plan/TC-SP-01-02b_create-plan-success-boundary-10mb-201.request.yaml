$kind: http-request
name: TC-SP-01-02b — File đúng 10MB → 201 (boundary pass)
url: '{{baseURL}}/api/v1/plans'
method: POST
headers:
  Authorization: Bearer {{accessToken}}
body:
  type: formdata
  content:
    - type: text
      key: name
      value: Ôn thi Giải tích Boundary
    - type: text
      key: deadline
      value: '2026-12-31'
    - key: file
      src: /home/minhphat/workspace/planning-ai/planning-ai/postman/test-data/TC-SP-Studyplanner/exactly_10mb.pdf
      type: file
      contentType: application/pdf
scripts:
  - type: afterResponse
    code: |-
      // TC-SP-01-02b: Boundary test — file đúng 10MB (10 × 1024 × 1024 bytes) phải PASS
      pm.test('TC-SP-01-02b: File đúng 10MB → 201 Created (boundary pass)', function () {
        pm.response.to.have.status(201);
      });

      const body = pm.response.json();

      pm.test('TC-SP-01-02b: success = true', function () {
        pm.expect(body.success).to.be.true;
      });

      pm.test('TC-SP-01-02b: data.plan.status = "draft"', function () {
        pm.expect(body.data.plan.status).to.equal('draft');
      });
    language: text/javascript
order: 2100
