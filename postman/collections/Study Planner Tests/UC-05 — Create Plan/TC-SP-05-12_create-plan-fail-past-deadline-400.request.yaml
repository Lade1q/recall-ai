$kind: http-request
name: TC-SP-05-12 — Tạo plan thất bại khi deadline trong quá khứ (400)
url: '{{baseURL}}/api/v1/study-plans'
method: POST
headers:
  Authorization: Bearer {{accessToken}}
body:
  type: formdata
  formdata:
    - key: name
      value: Ôn thi Lịch sử
    - key: deadline
      value: '2020-01-01'
    - key: file
      src: sample_lecture.pdf
      type: file
scripts:
  - type: afterResponse
    code: |-
      pm.test('TC-SP-05-12: Status code là 400 Bad Request', function () { pm.response.to.have.status(400); });
      const body = pm.response.json();
      pm.test('TC-SP-05-12: success = false', function () { pm.expect(body.success).to.be.false; });
      pm.test('TC-SP-05-12: error.code = VALIDATION_ERROR', function () { pm.expect(body.error.code).to.equal('VALIDATION_ERROR'); });
      pm.test('TC-SP-05-12: error.details đề cập deadline', function () {
        const details = JSON.stringify(body.error.details || body.error.message || '');
        pm.expect(details.toLowerCase()).to.include('deadline');
      });
    language: text/javascript
order: 12000
