$kind: http-request
name: TC-SP-05-11b — Tạo plan thất bại khi file PDF có mật khẩu (400)
url: '{{baseURL}}/api/v1/plans'
method: POST
headers:
  Authorization: Bearer {{accessToken}}
body:
  type: formdata
  content:
    - type: text
      key: name
      value: Ôn thi Hóa học (Protected PDF)
    - type: text
      key: deadline
      value: '2026-12-31'
    - key: file
      src: protected.pdf
      type: file
scripts:
  - type: afterResponse
    code: |-
      pm.test('TC-SP-05-11b: Status code là 400 hoặc 422', function () {
        pm.expect([400, 422]).to.include(pm.response.code);
      });
      const body = pm.response.json();
      pm.test('TC-SP-05-11b: success = false', function () { pm.expect(body.success).to.be.false; });
      pm.test('TC-SP-05-11b: error.code là FILE_CORRUPTED hoặc FILE_UNREADABLE', function () {
        pm.expect(['FILE_CORRUPTED', 'FILE_UNREADABLE', 'FILE_PROTECTED']).to.include(body.error.code);
      });
      pm.test('TC-SP-05-11b: error.message tồn tại', function () { pm.expect(body.error.message).to.be.a('string').and.not.empty; });
    language: text/javascript
order: 11100
