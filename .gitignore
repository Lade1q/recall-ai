.agents/
.claude/
.env
.env.local
.env.*.local
.local/
.gemini/

# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
out/

# Test artifacts (jest --coverage)
coverage/
e2e/playwright-report/
e2e/test-results/
e2e/blob-report/

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*

# Uploads
uploads/

# Local diagram-rendering tools (not SAD content)
tools/plantuml.jar
# Service-account key for the Vertex Live spike. Untracked and must stay that way:
# a `git add -A` at repo root would otherwise commit a live credential. Anchored to
# the root rather than a blanket *.json, which would swallow package.json and friends.
/recall-live-proxy-key.json
